/* ============================================================
   Tab3 — 리딩단지 Mock Data (더 이상 Tab3Leading에서 사용 안 함)
   실 데이터: public/data/leading-danji.json (MotherDuck 연동)
   ============================================================ */
export const MOCK_LEADING = [] as const;
